name: Deploy
on:
  repository_dispatch:
    types: [deploy]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Deploy
        uses: tomasnorre/ansible-cli-action@0.0.5
        with:
          command: "make deploy"
